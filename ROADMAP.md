# ST·主线设计 — 后续开发路线图（ROADMAP）

> 插件定位：SillyTavern / TauriTavern 的外部增量插件。读取角色卡（人设/世界观/开场白/卡内世界书）→ 生成一份可长期游玩的主线设计（全书方向 + 3–8 卷）→ 人审确认 → 转交 shujuku continuation 立纲，让 AI 长期沿一条固定主线推进。
>
> 边界：**不做逐回复注入、不做阶段游标**（那是 shujuku continuation 的职责）；不修改 shujuku 源码；不自动改写角色卡。

## 现状（v0.1.x 已落地）

- 材料包：深读完整角色卡、世界书条目读取（character_book / data.character_book）、token 预算裁剪、**条目勾选注入**
- 主线生成：OpenAI 兼容 API、结构严格校验、至多 2 轮增量修补、可读文档 / JSON 双视图审阅
- 持久化：按聊天存 `chat_metadata.extensions['st_mainline']`；可选写回角色卡扩展字段
- 转交：一键复制「立纲初始要求」，可直接粘贴为 continuation 初始要求
- 审视：只读偏差报告（符合 / 偏离 / 信息不足）
- UI：可展开/收起悬浮面板 + 可拖拽「主线」悬浮球（位置记忆）
- 双端兼容：SillyTavern 1.13+（st-context）优先，TauriTavern / TavernHelper 兜底

---

## P0 — 主线循环跑通（让"长期游玩"闭环）

**P0.1 主线跟随剧情自动更新（核心）**
- 目标：目前审视只"报告"不"行动"；要让主线真正"活"起来，偏离或剧情推进后应能一键让 LLM 修正卷方向，或建议切下一卷。
- 方案：新增「更新主线」动作 —— 基于【当前设计 + 最近正文 + 审视结论】调用 LLM 输出 `patch`（局部修订卷 direction/escalation/withheld），经 schema 校验后合并回设计并保存；保留修订前版本引用（不破坏已转交给 continuation 的总纲则需提示用户重新复制）。
- 涉及：`core/review.ts`（复用审视输入组装）、新增 `core/design-updater.ts`、`ui/tabs/design-tab.ts`。
- 验收：偏离报告为"偏离"时可一键得到修订版设计，校验通过、可保存、可重新复制立纲要求。

**P0.2 卷进度可感知**
- 目标：用户需要知道"现在玩到第几卷、这卷的目标是什么"。
- 方案：在「设计」页签顶部渲染卷进度条（active 卷高亮 + 后续卷灰置），数据来自设计本身（卷序 + completionState，若有）+ 可选读取 continuation 阶段完成数（P1.2 后可用）。
- 涉及：`core/design-doc.ts`（纯函数渲染进度模型）、`ui/tabs/design-tab.ts`。
- 验收：打开设计页签即可看到卷序列与当前所处的卷。

**P0.3 超时/失败恢复**
- 目标：生成/修补偶发失败不能丢用户输入。
- 方案：生成失败已把 lastRaw 载入 JSON 视图；增加「恢复上次草稿」入口（草稿存内存 + 失败即落 localStorage 草稿槽位）；API 配置错误给出可跳转指引。
- 验收：模拟网络失败后刷新页面仍可恢复草稿内容。

---

## P1 — 与 continuation 双向闭环

**P1.1 从"复制粘贴"升级为"一键交付"**
- 目标：减少手工步骤。
- 方案：尽力而为的自动填充 —— 若检测到 continuation 的输入区（DOM 探测，如同 embedded-runtime 的分类器思路），一键把立纲要求填入并提示创建任务；探测失败回退为复制。
- 风险：依赖 continuation 内部 DOM，属于 best-effort，需要 feature-detect + 降级，不承诺长期稳定性。
- 验收：在装了 shujuku 的酒馆中，一键后目标输入框出现立纲要求文本。

**P1.2 回读 continuation 进度（阶段完成数 → 卷进度）**
- 目标：主线设计与 continuation 实际进度不脱节。
- 方案：只读读取首楼 extra `_qrf_continuation`（continuation 信封），解析已完成阶段数映射到当前卷，展示在 P0.2 的进度条上；解析失败静默降级（不依赖该结构）。
- 涉及：新增 `core/continuation-snapshot.ts`（纯解析 + 容错）。
- 验收：有 continuation 任务的聊天中，进度条显示已推进阶段；无任务/结构变化时仍可用设计自身数据。

**P1.3 主线修订后与 continuation 对齐提示**
- 目标：P0.1 修订主线后，提醒用户"原总纲已发给 continuation，需重新立纲/粘贴"，避免两端不一致。
- 方案：修订保存时若检测到聊天中存在续写任务，给出明确提示与「复制新立纲要求」快捷入口。
- 验收：修订主线后用户收到提示并可一键复制。

---

## P2 — 体验与稳定性

**P2.1 多候选方案**
- 目标：生成 3 个风格差异化的主线候选，用户挑选后再进入审阅。
- 方案：生成参数 variant（如"冒险冒险节奏 / 日常羁绊 / 群像权谋"）；并行/顺序跑 2–3 次，结果并列展示，选中一个进入编辑器。
- 涉及：`core/mainline-generator.ts`、`ui/tabs/design-tab.ts`。
- 验收：一次点击可预览多个候选并选择其一。

**P2.2 卷/阶段可视化编辑**
- 目标：JSON textarea 编辑对非开发者不友好。
- 方案：卷卡片列表（标题/叙事角色/方向摘要），支持展开编辑（字段表单）、新增/删除/排序卷（排序会触发重新校验与立纲文本重渲染）。
- 验收：不打开 JSON 即可完成卷层面的增删改排。

**P2.3 世界书条目注入体验**
- 目标：大世界书下选择低效。
- 方案：按 comment/内容做关键词搜索过滤 + 一键"仅注入高相关"（可选：用 score 或让用户按主题打标）；显示已选 token 总和与预算占比。
- 涉及：`ui/tabs/material-tab.ts`、`shared/material-selection.ts`。
- 验收：100+ 条世界书时可快速定位并批量勾选。

**P2.4 自动化测试补强**
- 目标：后续改动安全网。
- 方案：为 generator 的修补路径、design-updater、continuation-snapshot、拖拽/折叠等纯逻辑抽离并补 vitest 用例；UI 事件绑定保持薄层。
- 验收：核心纯函数覆盖率明显提升，`npm test` 全绿。

**P2.5 主题与移动端适配**
- 目标：面板在暗/亮主题、窄屏（手机访问酒馆）下可用。
- 方案：跟随 `power_user.theme` 浅色变量（CSS 变量化）；窄屏下面板宽度自适应（max-width: 92vw）；悬浮球尺寸触屏友好。
- 涉及：`src/index.ts` / `src/ui/panel.ts` 样式改造。
- 验收：切换酒馆主题与窄窗口下视觉正常、可操作。

---

## P3 — 长期（探索性）

- **P3.1 主线创作向导**：角色卡读取后，先让用户回答 3–5 个问题（想玩多长/什么基调/排斥元素），再生成，提升首轮命中率。
- **P3.2 多角色/群聊适配**：支持以群聊全体角色共同生成主线。
- **P3.3 主线导出**：导出为角色卡扩展字段/独立 .md/可再导入的 JSON，便于分享与跨设备迁移。
- **P3.4 定期自动审视**：每 N 楼自动跑一次只读审视，偏离时通知用户（不出声打扰，仅面板红点/入口提示）。

---

## 明确不做（Non-Goals）

- 不做逐回复注入与阶段游标（交给 shujuku continuation）。
- 不做自动改写角色卡（写回需用户显式勾选）。
- 不承诺 continuation 内部 DOM 接口的长期稳定（P1.1 属 best-effort）。
- 不引入后端服务/数据库（保持纯前端扩展，数据在 chat metadata / localStorage）。

## 优先级建议

按"让长期游玩真正闭环"排序：P0.1 → P0.2 → P1.3 → P1.2 → P2.1 → P2.2 → P2.5 → P2.3 → P2.4（测试随时补充）→ P3。

## 版本规划

- v0.2.0：P0 全部
- v0.3.0：P1 全部
- v0.4.0：P2 全部
- 之后按需迭代 P3