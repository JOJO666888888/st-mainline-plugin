{
  "name": "st-mainline-plugin",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "ST·主线设计 — SillyTavern / TauriTavern 外部增量插件（配合 shujuku continuation 的『角色卡 → 主线设计』）。",
  "scripts": {
    "build": "rollup -c",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@rollup/plugin-typescript": "^12.1.0",
    "rollup": "^4.28.0",
    "tslib": "^2.8.0",
    "typescript": "^5.7.0",
    "vitest": "^3.2.4"
  }
}